---
title: 'Send mobile money'
description: 'Send money to mobile money accounts'
---
<Info>
  Supported by Basic, Pro & Pro+ Licenses  
  
  **[Learn more here](https://www.xente.co/pricing)**
</Info>

With the Xente API, you can send mobile money in three easy steps.
## Step 1 - Verify mobile money number

To ensure your sending money to the correct mobile money number, please first verify the number 

<CodeGroup>

```bash cURL
curl https://api.xente.co/auth/login?
-H "Authorization: Bearer YOUR_SECRET_KEY"
-X GET
```

```bash Node
const axios = require('axios');

const apiUrl = 'https://api.xente.co/verify?';

const options = {
  headers: {
    'Authorization': 'Bearer YOUR_SECRET_KEY'
  }
};

axios.get(apiUrl, options)
  .then(response => {
    // Handle the response data here
    console.log(response.data);
  })
  .catch(error => {
    // Handle any errors that occur
    console.error(error);
  });
```

```bash Java
const axios = require('axios');

const apiUrl = 'https://api.xente.co/verify?';

const options = {
  headers: {
Java code here
  }
};


```

```bash C#
const axios = require('axios');

const apiUrl = 'https://api.xente.co/verify?';

const options = {
 C# Code here
};


```

```bash PHP
const axios = require('axios');

const apiUrl = 'https://api.xente.co/verify?';

const options = {
  headers: {
    PHP Code here 
  }
};

```
</CodeGroup>

P.S. We advise that you store or cache verified mobile money numbers in your backend to avoid making verifications on every subsequent request

## Step 2 - POST mobile money transaction

You POST a mobile money transaction as below  

<CodeGroup>

```bash cURL
curl https://api.xente.co/auth/login?
-H "Authorization: Bearer YOUR_SECRET_KEY"
-X GET
```

```bash Node
const axios = require('axios');

const apiUrl = 'https://api.xente.co/verify?';

const options = {
  headers: {
    'Authorization': 'Bearer YOUR_SECRET_KEY'
  }
};

axios.get(apiUrl, options)
  .then(response => {
    // Handle the response data here
    console.log(response.data);
  })
  .catch(error => {
    // Handle any errors that occur
    console.error(error);
  });
```

```bash Java
const axios = require('axios');

const apiUrl = 'https://api.xente.co/verify?';

const options = {
  headers: {
Java code here
  }
};


```

```bash C#
const axios = require('axios');

const apiUrl = 'https://api.xente.co/verify?';

const options = {
 C# Code here
};


```

```bash PHP
const axios = require('axios');

const apiUrl = 'https://api.xente.co/verify?';

const options = {
  headers: {
    PHP Code here 
  }
};

```
</CodeGroup>

## Step 3 - Receive Instant Payment Notification (IPN)

All transactions on Xente are asynchronous. Therefore, after you POST a mobile money transaction, you need to wait for a success (or failure) response using webhooks called Instant Payment Notifications (IPN).

Learn more about **[Instant Payment Notifications](/start/ipn)**.