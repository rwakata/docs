---
title: 'Manage cards'
description: 'Add funds, get balance, view transactions, activate, and deactivate cards.'
---

<Info>
  **Prerequisite** You need a Pro+ account to create virtual Visa debit cards API
</Info>

With the Xente API, you can easily handle card management tasks right from your own application. This includes simple actions like adding funds, checking card balances, reviewing transaction history, and toggling card activation status.

## Add funds to physical and virtual Visa debit cards

To add funds to individual virtual and physical Visa cards, utilize the following API. It's important to note that the funds will be transferred from your main account balance to the respective individual card account.

<CodeGroup>

```bash cURL
curl -X POST -H "Content-Type: application/json" -H "Authorization: Bearer YOUR_AUTH_TOKEN" -d '{
  "user_email": "user@email.com",
  "thirdpartyid": "190890",//this is an id from your platform to id the user
}' "https://api.xente.co/virtual-cards"

```

```bash Node
const axios = require('axios');

const headers = {
  'Content-Type': 'application/json',
  'Authorization': 'Bearer YOUR_AUTH_TOKEN'
};

const data = {
  user_email: 'user@email.com',
  thirdpartyid: '190890'
};

axios.post('https://api.xente.co/cards', data, { headers })
  .then(response => {
    console.log(response.data);
  })
  .catch(error => {
    console.error(error);
  });
```

```bash Java

import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.io.OutputStream;
import java.net.HttpURLConnection;
import java.net.URL;

public class Main {
    public static void main(String[] args) throws Exception {
        String url = "https://api.xente.co/cards";
        String authHeader = "Bearer YOUR_AUTH_TOKEN";
        String requestBody = "{\n" +
                "  \"user_email\": \"user@email.com\",\n" +
                "  \"thirdpartyid\": \"190890\"\n" +
                "}";

        URL obj = new URL(url);
        HttpURLConnection con = (HttpURLConnection) obj.openConnection();

        // Set the request method to POST
        con.setRequestMethod("POST");

        // Set the request headers
        con.setRequestProperty("Content-Type", "application/json");
        con.setRequestProperty("Authorization", authHeader);

        // Enable sending request body
        con.setDoOutput(true);

        // Write the request body
        OutputStream outputStream = con.getOutputStream();
        outputStream.write(requestBody.getBytes());
        outputStream.flush();
        outputStream.close();

        // Get the response
        int responseCode = con.getResponseCode();
        BufferedReader in = new BufferedReader(new InputStreamReader(con.getInputStream()));
        String inputLine;
        StringBuilder response = new StringBuilder();
        while ((inputLine = in.readLine()) != null) {
            response.append(inputLine);
        }
        in.close();

        // Print the response
        System.out.println("Response Code: " + responseCode);
        System.out.println("Response Body: " + response.toString());
    }
}


```

```bash C#

using System;
using System.Net.Http;
using System.Net.Http.Headers;

class Program
{
    static async System.Threading.Tasks.Task Main(string[] args)
    {
        string url = "https://api.xente.co/cards";
        string authToken = "YOUR_AUTH_TOKEN";

        using (HttpClient client = new HttpClient())
        {
            client.DefaultRequestHeaders.Authorization = new AuthenticationHeaderValue("Bearer", authToken);
            client.DefaultRequestHeaders.Accept.Add(new MediaTypeWithQualityHeaderValue("application/json"));

            var requestData = new
            {
                user_email = "user@email.com",
                thirdpartyid = "190890"
            };

            var content = new StringContent(Newtonsoft.Json.JsonConvert.SerializeObject(requestData));
            content.Headers.ContentType = new MediaTypeHeaderValue("application/json");

            var response = await client.PostAsync(url, content);

            if (response.IsSuccessStatusCode)
            {
                var result = await response.Content.ReadAsStringAsync();
                Console.WriteLine(result);
            }
            else
            {
                Console.WriteLine("Request failed with status code: " + response.StatusCode);
            }
        }
    }
}


```

```bash PHP

<?php

$url = "https://api.xente.co/cards";
$headers = array(
    "Content-Type: application/json",
    "Authorization: Bearer YOUR_AUTH_TOKEN"
);

$data = array(
    "user_email" => "user@email.com",
    "thirdpartyid" => "190890"
);

$ch = curl_init($url);
curl_setopt($ch, CURLOPT_POST, true);
curl_setopt($ch, CURLOPT_POSTFIELDS, json_encode($data));
curl_setopt($ch, CURLOPT_HTTPHEADER, $headers);
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);

$response = curl_exec($ch);
curl_close($ch);

echo $response;

?>

```
</CodeGroup>

## Get Visa debit card balances

To retrieve card balances, utilize the following API.

<CodeGroup>

```bash cURL
curl -X GET -H "Content-Type: application/json" -H "Authorization: Bearer YOUR_AUTH_TOKEN" -d '{
  "user_email": "user@email.com",
  "thirdpartyid": "190890",//this is an id from your platform to id the user
}' "https://api.xente.co/virtual-cards"

```

```bash Node
const axios = require('axios');

const headers = {
  'Content-Type': 'application/json',
  'Authorization': 'Bearer YOUR_AUTH_TOKEN'
};

const data = {
  user_email: 'user@email.com',
  thirdpartyid: '190890'
};

axios.post('https://api.xente.co/cards', data, { headers })
  .then(response => {
    console.log(response.data);
  })
  .catch(error => {
    console.error(error);
  });
```

```bash Java

import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.io.OutputStream;
import java.net.HttpURLConnection;
import java.net.URL;

public class Main {
    public static void main(String[] args) throws Exception {
        String url = "https://api.xente.co/cards";
        String authHeader = "Bearer YOUR_AUTH_TOKEN";
        String requestBody = "{\n" +
                "  \"user_email\": \"user@email.com\",\n" +
                "  \"thirdpartyid\": \"190890\"\n" +
                "}";

        URL obj = new URL(url);
        HttpURLConnection con = (HttpURLConnection) obj.openConnection();

        // Set the request method to POST
        con.setRequestMethod("POST");

        // Set the request headers
        con.setRequestProperty("Content-Type", "application/json");
        con.setRequestProperty("Authorization", authHeader);

        // Enable sending request body
        con.setDoOutput(true);

        // Write the request body
        OutputStream outputStream = con.getOutputStream();
        outputStream.write(requestBody.getBytes());
        outputStream.flush();
        outputStream.close();

        // Get the response
        int responseCode = con.getResponseCode();
        BufferedReader in = new BufferedReader(new InputStreamReader(con.getInputStream()));
        String inputLine;
        StringBuilder response = new StringBuilder();
        while ((inputLine = in.readLine()) != null) {
            response.append(inputLine);
        }
        in.close();

        // Print the response
        System.out.println("Response Code: " + responseCode);
        System.out.println("Response Body: " + response.toString());
    }
}


```

```bash C#

using System;
using System.Net.Http;
using System.Net.Http.Headers;

class Program
{
    static async System.Threading.Tasks.Task Main(string[] args)
    {
        string url = "https://api.xente.co/cards";
        string authToken = "YOUR_AUTH_TOKEN";

        using (HttpClient client = new HttpClient())
        {
            client.DefaultRequestHeaders.Authorization = new AuthenticationHeaderValue("Bearer", authToken);
            client.DefaultRequestHeaders.Accept.Add(new MediaTypeWithQualityHeaderValue("application/json"));

            var requestData = new
            {
                user_email = "user@email.com",
                thirdpartyid = "190890"
            };

            var content = new StringContent(Newtonsoft.Json.JsonConvert.SerializeObject(requestData));
            content.Headers.ContentType = new MediaTypeHeaderValue("application/json");

            var response = await client.PostAsync(url, content);

            if (response.IsSuccessStatusCode)
            {
                var result = await response.Content.ReadAsStringAsync();
                Console.WriteLine(result);
            }
            else
            {
                Console.WriteLine("Request failed with status code: " + response.StatusCode);
            }
        }
    }
}


```

```bash PHP

<?php

$url = "https://api.xente.co/cards";
$headers = array(
    "Content-Type: application/json",
    "Authorization: Bearer YOUR_AUTH_TOKEN"
);

$data = array(
    "user_email" => "user@email.com",
    "thirdpartyid" => "190890"
);

$ch = curl_init($url);
curl_setopt($ch, CURLOPT_POST, true);
curl_setopt($ch, CURLOPT_POSTFIELDS, json_encode($data));
curl_setopt($ch, CURLOPT_HTTPHEADER, $headers);
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);

$response = curl_exec($ch);
curl_close($ch);

echo $response;

?>

```
</CodeGroup>

## Get Visa debit card transaction history

To retrieve card transactions, utilize the following API.

<CodeGroup>

```bash cURL
curl -X GET -H "Content-Type: application/json" -H "Authorization: Bearer YOUR_AUTH_TOKEN" -d '{
  "user_email": "user@email.com",
  "thirdpartyid": "190890",//this is an id from your platform to id the user
}' "https://api.xente.co/virtual-cards"

```

```bash Node
const axios = require('axios');

const headers = {
  'Content-Type': 'application/json',
  'Authorization': 'Bearer YOUR_AUTH_TOKEN'
};

const data = {
  user_email: 'user@email.com',
  thirdpartyid: '190890'
};

axios.post('https://api.xente.co/cards', data, { headers })
  .then(response => {
    console.log(response.data);
  })
  .catch(error => {
    console.error(error);
  });
```

```bash Java

import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.io.OutputStream;
import java.net.HttpURLConnection;
import java.net.URL;

public class Main {
    public static void main(String[] args) throws Exception {
        String url = "https://api.xente.co/cards";
        String authHeader = "Bearer YOUR_AUTH_TOKEN";
        String requestBody = "{\n" +
                "  \"user_email\": \"user@email.com\",\n" +
                "  \"thirdpartyid\": \"190890\"\n" +
                "}";

        URL obj = new URL(url);
        HttpURLConnection con = (HttpURLConnection) obj.openConnection();

        // Set the request method to POST
        con.setRequestMethod("POST");

        // Set the request headers
        con.setRequestProperty("Content-Type", "application/json");
        con.setRequestProperty("Authorization", authHeader);

        // Enable sending request body
        con.setDoOutput(true);

        // Write the request body
        OutputStream outputStream = con.getOutputStream();
        outputStream.write(requestBody.getBytes());
        outputStream.flush();
        outputStream.close();

        // Get the response
        int responseCode = con.getResponseCode();
        BufferedReader in = new BufferedReader(new InputStreamReader(con.getInputStream()));
        String inputLine;
        StringBuilder response = new StringBuilder();
        while ((inputLine = in.readLine()) != null) {
            response.append(inputLine);
        }
        in.close();

        // Print the response
        System.out.println("Response Code: " + responseCode);
        System.out.println("Response Body: " + response.toString());
    }
}


```

```bash C#

using System;
using System.Net.Http;
using System.Net.Http.Headers;

class Program
{
    static async System.Threading.Tasks.Task Main(string[] args)
    {
        string url = "https://api.xente.co/cards";
        string authToken = "YOUR_AUTH_TOKEN";

        using (HttpClient client = new HttpClient())
        {
            client.DefaultRequestHeaders.Authorization = new AuthenticationHeaderValue("Bearer", authToken);
            client.DefaultRequestHeaders.Accept.Add(new MediaTypeWithQualityHeaderValue("application/json"));

            var requestData = new
            {
                user_email = "user@email.com",
                thirdpartyid = "190890"
            };

            var content = new StringContent(Newtonsoft.Json.JsonConvert.SerializeObject(requestData));
            content.Headers.ContentType = new MediaTypeHeaderValue("application/json");

            var response = await client.PostAsync(url, content);

            if (response.IsSuccessStatusCode)
            {
                var result = await response.Content.ReadAsStringAsync();
                Console.WriteLine(result);
            }
            else
            {
                Console.WriteLine("Request failed with status code: " + response.StatusCode);
            }
        }
    }
}


```

```bash PHP

<?php

$url = "https://api.xente.co/cards";
$headers = array(
    "Content-Type: application/json",
    "Authorization: Bearer YOUR_AUTH_TOKEN"
);

$data = array(
    "user_email" => "user@email.com",
    "thirdpartyid" => "190890"
);

$ch = curl_init($url);
curl_setopt($ch, CURLOPT_POST, true);
curl_setopt($ch, CURLOPT_POSTFIELDS, json_encode($data));
curl_setopt($ch, CURLOPT_HTTPHEADER, $headers);
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);

$response = curl_exec($ch);
curl_close($ch);

echo $response;

?>

```
</CodeGroup>

## Get Activate a Visa debit card

To a Visa debit card, utilize the following API.

<CodeGroup>

```bash cURL
curl -X GET -H "Content-Type: application/json" -H "Authorization: Bearer YOUR_AUTH_TOKEN" -d '{
  "user_email": "user@email.com",
  "thirdpartyid": "190890",//this is an id from your platform to id the user
}' "https://api.xente.co/virtual-cards"

```

```bash Node
const axios = require('axios');

const headers = {
  'Content-Type': 'application/json',
  'Authorization': 'Bearer YOUR_AUTH_TOKEN'
};

const data = {
  user_email: 'user@email.com',
  thirdpartyid: '190890'
};

axios.post('https://api.xente.co/cards', data, { headers })
  .then(response => {
    console.log(response.data);
  })
  .catch(error => {
    console.error(error);
  });
```

```bash Java

import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.io.OutputStream;
import java.net.HttpURLConnection;
import java.net.URL;

public class Main {
    public static void main(String[] args) throws Exception {
        String url = "https://api.xente.co/cards";
        String authHeader = "Bearer YOUR_AUTH_TOKEN";
        String requestBody = "{\n" +
                "  \"user_email\": \"user@email.com\",\n" +
                "  \"thirdpartyid\": \"190890\"\n" +
                "}";

        URL obj = new URL(url);
        HttpURLConnection con = (HttpURLConnection) obj.openConnection();

        // Set the request method to POST
        con.setRequestMethod("POST");

        // Set the request headers
        con.setRequestProperty("Content-Type", "application/json");
        con.setRequestProperty("Authorization", authHeader);

        // Enable sending request body
        con.setDoOutput(true);

        // Write the request body
        OutputStream outputStream = con.getOutputStream();
        outputStream.write(requestBody.getBytes());
        outputStream.flush();
        outputStream.close();

        // Get the response
        int responseCode = con.getResponseCode();
        BufferedReader in = new BufferedReader(new InputStreamReader(con.getInputStream()));
        String inputLine;
        StringBuilder response = new StringBuilder();
        while ((inputLine = in.readLine()) != null) {
            response.append(inputLine);
        }
        in.close();

        // Print the response
        System.out.println("Response Code: " + responseCode);
        System.out.println("Response Body: " + response.toString());
    }
}


```

```bash C#

using System;
using System.Net.Http;
using System.Net.Http.Headers;

class Program
{
    static async System.Threading.Tasks.Task Main(string[] args)
    {
        string url = "https://api.xente.co/cards";
        string authToken = "YOUR_AUTH_TOKEN";

        using (HttpClient client = new HttpClient())
        {
            client.DefaultRequestHeaders.Authorization = new AuthenticationHeaderValue("Bearer", authToken);
            client.DefaultRequestHeaders.Accept.Add(new MediaTypeWithQualityHeaderValue("application/json"));

            var requestData = new
            {
                user_email = "user@email.com",
                thirdpartyid = "190890"
            };

            var content = new StringContent(Newtonsoft.Json.JsonConvert.SerializeObject(requestData));
            content.Headers.ContentType = new MediaTypeHeaderValue("application/json");

            var response = await client.PostAsync(url, content);

            if (response.IsSuccessStatusCode)
            {
                var result = await response.Content.ReadAsStringAsync();
                Console.WriteLine(result);
            }
            else
            {
                Console.WriteLine("Request failed with status code: " + response.StatusCode);
            }
        }
    }
}


```

```bash PHP

<?php

$url = "https://api.xente.co/cards";
$headers = array(
    "Content-Type: application/json",
    "Authorization: Bearer YOUR_AUTH_TOKEN"
);

$data = array(
    "user_email" => "user@email.com",
    "thirdpartyid" => "190890"
);

$ch = curl_init($url);
curl_setopt($ch, CURLOPT_POST, true);
curl_setopt($ch, CURLOPT_POSTFIELDS, json_encode($data));
curl_setopt($ch, CURLOPT_HTTPHEADER, $headers);
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);

$response = curl_exec($ch);
curl_close($ch);

echo $response;

?>

```
</CodeGroup>

## Deactivate a Visa debit card

To deactivate a Visa card , utilize the following API.

<CodeGroup>

```bash cURL
curl -X GET -H "Content-Type: application/json" -H "Authorization: Bearer YOUR_AUTH_TOKEN" -d '{
  "user_email": "user@email.com",
  "thirdpartyid": "190890",//this is an id from your platform to id the user
}' "https://api.xente.co/virtual-cards"

```

```bash Node
const axios = require('axios');

const headers = {
  'Content-Type': 'application/json',
  'Authorization': 'Bearer YOUR_AUTH_TOKEN'
};

const data = {
  user_email: 'user@email.com',
  thirdpartyid: '190890'
};

axios.post('https://api.xente.co/cards', data, { headers })
  .then(response => {
    console.log(response.data);
  })
  .catch(error => {
    console.error(error);
  });
```

```bash Java

import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.io.OutputStream;
import java.net.HttpURLConnection;
import java.net.URL;

public class Main {
    public static void main(String[] args) throws Exception {
        String url = "https://api.xente.co/cards";
        String authHeader = "Bearer YOUR_AUTH_TOKEN";
        String requestBody = "{\n" +
                "  \"user_email\": \"user@email.com\",\n" +
                "  \"thirdpartyid\": \"190890\"\n" +
                "}";

        URL obj = new URL(url);
        HttpURLConnection con = (HttpURLConnection) obj.openConnection();

        // Set the request method to POST
        con.setRequestMethod("POST");

        // Set the request headers
        con.setRequestProperty("Content-Type", "application/json");
        con.setRequestProperty("Authorization", authHeader);

        // Enable sending request body
        con.setDoOutput(true);

        // Write the request body
        OutputStream outputStream = con.getOutputStream();
        outputStream.write(requestBody.getBytes());
        outputStream.flush();
        outputStream.close();

        // Get the response
        int responseCode = con.getResponseCode();
        BufferedReader in = new BufferedReader(new InputStreamReader(con.getInputStream()));
        String inputLine;
        StringBuilder response = new StringBuilder();
        while ((inputLine = in.readLine()) != null) {
            response.append(inputLine);
        }
        in.close();

        // Print the response
        System.out.println("Response Code: " + responseCode);
        System.out.println("Response Body: " + response.toString());
    }
}


```

```bash C#

using System;
using System.Net.Http;
using System.Net.Http.Headers;

class Program
{
    static async System.Threading.Tasks.Task Main(string[] args)
    {
        string url = "https://api.xente.co/cards";
        string authToken = "YOUR_AUTH_TOKEN";

        using (HttpClient client = new HttpClient())
        {
            client.DefaultRequestHeaders.Authorization = new AuthenticationHeaderValue("Bearer", authToken);
            client.DefaultRequestHeaders.Accept.Add(new MediaTypeWithQualityHeaderValue("application/json"));

            var requestData = new
            {
                user_email = "user@email.com",
                thirdpartyid = "190890"
            };

            var content = new StringContent(Newtonsoft.Json.JsonConvert.SerializeObject(requestData));
            content.Headers.ContentType = new MediaTypeHeaderValue("application/json");

            var response = await client.PostAsync(url, content);

            if (response.IsSuccessStatusCode)
            {
                var result = await response.Content.ReadAsStringAsync();
                Console.WriteLine(result);
            }
            else
            {
                Console.WriteLine("Request failed with status code: " + response.StatusCode);
            }
        }
    }
}


```

```bash PHP

<?php

$url = "https://api.xente.co/cards";
$headers = array(
    "Content-Type: application/json",
    "Authorization: Bearer YOUR_AUTH_TOKEN"
);

$data = array(
    "user_email" => "user@email.com",
    "thirdpartyid" => "190890"
);

$ch = curl_init($url);
curl_setopt($ch, CURLOPT_POST, true);
curl_setopt($ch, CURLOPT_POSTFIELDS, json_encode($data));
curl_setopt($ch, CURLOPT_HTTPHEADER, $headers);
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);

$response = curl_exec($ch);
curl_close($ch);

echo $response;

?>

```
</CodeGroup>

## Set PIN

To set a card PIN, utilize the following API.

<CodeGroup>

```bash cURL
curl -X GET -H "Content-Type: application/json" -H "Authorization: Bearer YOUR_AUTH_TOKEN" -d '{
  "user_email": "user@email.com",
  "thirdpartyid": "190890",//this is an id from your platform to id the user
}' "https://api.xente.co/virtual-cards"

```

```bash Node
const axios = require('axios');

const headers = {
  'Content-Type': 'application/json',
  'Authorization': 'Bearer YOUR_AUTH_TOKEN'
};

const data = {
  user_email: 'user@email.com',
  thirdpartyid: '190890'
};

axios.post('https://api.xente.co/cards', data, { headers })
  .then(response => {
    console.log(response.data);
  })
  .catch(error => {
    console.error(error);
  });
```

```bash Java

import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.io.OutputStream;
import java.net.HttpURLConnection;
import java.net.URL;

public class Main {
    public static void main(String[] args) throws Exception {
        String url = "https://api.xente.co/cards";
        String authHeader = "Bearer YOUR_AUTH_TOKEN";
        String requestBody = "{\n" +
                "  \"user_email\": \"user@email.com\",\n" +
                "  \"thirdpartyid\": \"190890\"\n" +
                "}";

        URL obj = new URL(url);
        HttpURLConnection con = (HttpURLConnection) obj.openConnection();

        // Set the request method to POST
        con.setRequestMethod("POST");

        // Set the request headers
        con.setRequestProperty("Content-Type", "application/json");
        con.setRequestProperty("Authorization", authHeader);

        // Enable sending request body
        con.setDoOutput(true);

        // Write the request body
        OutputStream outputStream = con.getOutputStream();
        outputStream.write(requestBody.getBytes());
        outputStream.flush();
        outputStream.close();

        // Get the response
        int responseCode = con.getResponseCode();
        BufferedReader in = new BufferedReader(new InputStreamReader(con.getInputStream()));
        String inputLine;
        StringBuilder response = new StringBuilder();
        while ((inputLine = in.readLine()) != null) {
            response.append(inputLine);
        }
        in.close();

        // Print the response
        System.out.println("Response Code: " + responseCode);
        System.out.println("Response Body: " + response.toString());
    }
}


```

```bash C#

using System;
using System.Net.Http;
using System.Net.Http.Headers;

class Program
{
    static async System.Threading.Tasks.Task Main(string[] args)
    {
        string url = "https://api.xente.co/cards";
        string authToken = "YOUR_AUTH_TOKEN";

        using (HttpClient client = new HttpClient())
        {
            client.DefaultRequestHeaders.Authorization = new AuthenticationHeaderValue("Bearer", authToken);
            client.DefaultRequestHeaders.Accept.Add(new MediaTypeWithQualityHeaderValue("application/json"));

            var requestData = new
            {
                user_email = "user@email.com",
                thirdpartyid = "190890"
            };

            var content = new StringContent(Newtonsoft.Json.JsonConvert.SerializeObject(requestData));
            content.Headers.ContentType = new MediaTypeHeaderValue("application/json");

            var response = await client.PostAsync(url, content);

            if (response.IsSuccessStatusCode)
            {
                var result = await response.Content.ReadAsStringAsync();
                Console.WriteLine(result);
            }
            else
            {
                Console.WriteLine("Request failed with status code: " + response.StatusCode);
            }
        }
    }
}


```

```bash PHP

<?php

$url = "https://api.xente.co/cards";
$headers = array(
    "Content-Type: application/json",
    "Authorization: Bearer YOUR_AUTH_TOKEN"
);

$data = array(
    "user_email" => "user@email.com",
    "thirdpartyid" => "190890"
);

$ch = curl_init($url);
curl_setopt($ch, CURLOPT_POST, true);
curl_setopt($ch, CURLOPT_POSTFIELDS, json_encode($data));
curl_setopt($ch, CURLOPT_HTTPHEADER, $headers);
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);

$response = curl_exec($ch);
curl_close($ch);

echo $response;

?>

```
</CodeGroup>